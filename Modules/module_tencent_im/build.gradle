android {
    if (!ConfigUtils.isApply(project)) {
        defaultConfig {
            applicationId "com.example.william.my.module.tencent.im"
            multiDexEnabled true
            ndk {
                abiFilters "arm64-v8a"
            }
        }
        buildTypes {
            debug {
                //signingConfig signingConfigs.sample
            }
        }
    }
    resourcePrefix "im_"
}
dependencies {
    implementation deps.gson
    //
    implementation deps.imsdk
    implementation deps.imsdk_tuikit
}