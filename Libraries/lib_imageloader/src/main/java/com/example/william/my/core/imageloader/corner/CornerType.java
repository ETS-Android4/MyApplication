package com.example.william.my.core.imageloader.corner;

public enum CornerType {
    /**
     * 所有角
     */
    ALL,
    /**
     * 上左，上右，下左，下右
     */
    TOP_LEFT, TOP_RIGHT, BOTTOM_LEFT, BOTTOM_RIGHT,
    /**
     * 上侧，下侧，左侧，右侧
     */
    TOP, BOTTOM, LEFT, RIGHT,
}